import Testing
@testable import MuxTCP

@Suite("TCP connection placeholder")
struct TCPConnectionTests {
  @Test("Placeholder — TCP target compiles")
  func compiles() {
    // This test exists to verify the MuxTCP target compiles.
    // Real tests will be added in Phase 2.
    #expect(true)
  }
}
